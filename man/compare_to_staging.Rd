% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_to_staging.R
\name{compare_to_staging}
\alias{compare_to_staging}
\title{Compare a gene signature against clinical staging (TNM/AJCC)}
\usage{
compare_to_staging(
  scored,
  surv_time,
  surv_event,
  stage,
  stage_grouping = "auto"
)
}
\arguments{
\item{scored}{A \code{ScoredSignature} object from \code{score_signature()}}

\item{surv_time}{Numeric vector of survival times}

\item{surv_event}{Numeric vector: 1 = event, 0 = censored}

\item{stage}{Factor or character vector of clinical stages for each patient.
Accepted formats: "I","II","III","IV" or "Stage I","Stage II", etc.
Numeric values 1-4 are also accepted.}

\item{stage_grouping}{How to group stages for survival analysis:
\code{"auto"} (default): I+II = Early, III+IV = Late
\code{"each"}: treat each stage separately}
}
\value{
A list of class \code{StagingComparison} with C-index and
log-rank results for both the signature and clinical staging
}
\description{
Compare a gene signature against clinical staging (TNM/AJCC)
}
\examples{
set.seed(42)
mat <- matrix(rnorm(500), nrow = 5,
              dimnames = list(c("TP53","KRAS","MYC","CDKN2A","SMAD4"),
                              paste0("Patient_", 1:100)))
sig    <- load_signature(c("TP53","KRAS","MYC"), "TestSig", "PAAD")
scored <- score_signature(sig, mat)
surv_t <- rexp(100, rate = 0.05)
surv_e <- rbinom(100, 1, 0.7)
stages <- sample(c("I","II","III","IV"), 100, replace = TRUE)
result <- compare_to_staging(scored, surv_t, surv_e, stages)
print(result)
}
