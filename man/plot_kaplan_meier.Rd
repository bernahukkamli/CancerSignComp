% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_kaplan_meier.R
\name{plot_kaplan_meier}
\alias{plot_kaplan_meier}
\title{Plot Kaplan-Meier survival curves for a scored signature}
\usage{
plot_kaplan_meier(
  scored,
  surv_time,
  surv_event,
  time_unit = "Months",
  title = NULL,
  palette = "jco",
  risk_table = TRUE,
  conf_int = TRUE
)
}
\arguments{
\item{scored}{A \code{ScoredSignature} object from \code{score_signature()}}

\item{surv_time}{Numeric vector of survival times}

\item{surv_event}{Numeric vector: 1 = event, 0 = censored}

\item{time_unit}{Label for the x-axis time unit (default: "Months")}

\item{title}{Optional plot title. If NULL, uses signature name.}

\item{palette}{Color palette: "jco" (default), "lancet", "nejm", "aaas"}

\item{risk_table}{Logical: show risk table below plot (default: TRUE)}

\item{conf_int}{Logical: show confidence intervals (default: TRUE)}
}
\value{
A \code{ggsurvplot} object (printable and saveable)
}
\description{
Plot Kaplan-Meier survival curves for a scored signature
}
\examples{
set.seed(42)
mat <- matrix(rnorm(500), nrow = 5,
              dimnames = list(c("TP53","KRAS","MYC","CDKN2A","SMAD4"),
                              paste0("Patient_", 1:100)))
sig    <- load_signature(c("TP53","KRAS","MYC"), "TestSig", "PAAD")
scored <- score_signature(sig, mat)
surv_t <- rexp(100, rate = 0.05)
surv_e <- rbinom(100, 1, 0.7)
p <- plot_kaplan_meier(scored, surv_t, surv_e)
print(p)
}
